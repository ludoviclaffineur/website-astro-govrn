---
const researchPhases = [
  {
    title: "Data Collection",
    icon: "📊",
    description: "A comprehensive multi-method approach to gather diverse perspectives and insights.",
    methods: [
      {
        name: "In-depth Interviews",
        details: "One-on-one sessions with board members and executives",
        count: "50+ interviews"
      },
      {
        name: "Survey Analysis",
        details: "Quantitative data collection across organizations",
        count: "200+ responses"
      },
      {
        name: "Case Studies",
        details: "Detailed examination of AI implementation success stories",
        count: "15 cases"
      }
    ]
  },
  {
    title: "Research Scope",
    icon: "🎯",
    description: "Carefully selected participants across various sectors and roles.",
    participants: [
      {
        role: "Board Leadership",
        positions: ["Board Chairs", "Committee Heads", "Lead Directors"],
        percentage: "35%"
      },
      {
        role: "Executive Management",
        positions: ["CEOs", "CFOs", "CIOs"],
        percentage: "40%"
      },
      {
        role: "Governance Professionals",
        positions: ["Corporate Secretaries", "Risk Officers", "Compliance Managers"],
        percentage: "25%"
      }
    ]
  },
  {
    title: "Analysis Framework",
    icon: "🔍",
    description: "Rigorous analytical approach combining qualitative and quantitative methods.",
    components: [
      {
        name: "Thematic Analysis",
        description: "Identification of key patterns and trends in adoption challenges"
      },
      {
        name: "Statistical Validation",
        description: "Quantitative verification of findings and correlations"
      },
      {
        name: "Cross-Sector Comparison",
        description: "Analysis of variations across different industries and regions"
      }
    ]
  }
];
---
<section class="py-16 bg-gray-50">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2 class="text-3xl font-bold text-gray-900 mb-4">Research Methodology</h2>
      <p class="text-xl text-gray-600 max-w-2xl mx-auto">
        Our comprehensive research approach combines rigorous academic methods with practical industry insights to ensure actionable findings.
      </p>
    </div>

    <div class="space-y-12">
      {researchPhases.map(phase => (
        <div class="bg-white rounded-2xl p-8 shadow-lg border border-gray-100">
          <div class="flex items-start space-x-4 mb-8">
            <div class="flex-shrink-0 w-12 h-12 bg-primary/10 rounded-xl flex items-center justify-center text-2xl">
              {phase.icon}
            </div>
            <div>
              <h3 class="text-2xl font-bold text-gray-900 mb-2">{phase.title}</h3>
              <p class="text-gray-600">{phase.description}</p>
            </div>
          </div>

          {phase.methods && (
            <div class="grid md:grid-cols-3 gap-6">
              {phase.methods.map(method => (
                <div class="bg-gradient-to-br from-primary/5 to-blue-500/5 rounded-xl p-6 border border-primary/10">
                  <div class="text-lg font-semibold text-primary mb-2">{method.name}</div>
                  <p class="text-gray-600 text-sm mb-4">{method.details}</p>
                  <div class="text-sm font-medium text-primary bg-primary/10 rounded-full px-3 py-1 inline-block">
                    {method.count}
                  </div>
                </div>
              ))}
            </div>
          )}

          {phase.participants && (
            <div class="space-y-6">
              {phase.participants.map(participant => (
                <div class="bg-gradient-to-br from-primary/5 to-blue-500/5 rounded-xl p-6 border border-primary/10">
                  <div class="flex justify-between items-center mb-4">
                    <div class="text-lg font-semibold text-primary">{participant.role}</div>
                    <div class="text-sm font-medium text-primary bg-primary/10 rounded-full px-3 py-1">
                      {participant.percentage}
                    </div>
                  </div>
                  <div class="flex flex-wrap gap-2">
                    {participant.positions.map(position => (
                      <span class="text-sm text-gray-600 bg-white/50 rounded-full px-3 py-1">
                        {position}
                      </span>
                    ))}
                  </div>
                </div>
              ))}
            </div>
          )}

          {phase.components && (
            <div class="grid md:grid-cols-3 gap-6">
              {phase.components.map(component => (
                <div class="bg-gradient-to-br from-primary/5 to-blue-500/5 rounded-xl p-6 border border-primary/10">
                  <div class="text-lg font-semibold text-primary mb-2">{component.name}</div>
                  <p class="text-gray-600 text-sm">{component.description}</p>
                </div>
              ))}
            </div>
          )}
        </div>
      ))}
    </div>
  </div>
</section>